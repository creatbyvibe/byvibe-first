// byvibe.ai Hero Page - Animation Controller
// Handles page load animations and interactions

document.addEventListener('DOMContentLoaded', function() {
    // All animations are handled via CSS animations
    // This script can be extended for additional interactions if needed
    
    // Add smooth scroll behavior
    document.documentElement.style.scrollBehavior = 'smooth';
    
    // Optional: Add intersection observer for scroll-triggered animations
    // (Not needed for this single-page hero, but kept for future extensibility)
    
    console.log('byvibe.ai - Hero page loaded');
});
